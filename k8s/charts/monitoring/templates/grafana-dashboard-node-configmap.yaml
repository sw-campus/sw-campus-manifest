{{- if .Values.configMaps.grafanaDashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.global.namespace }}-grafana-dashboard-node
  labels:
    grafana_dashboard: "1"
data:
  node-exporter.json: |
{{ .Files.Get "dashboards/node-exporter.json" | indent 4 }}
{{- end }}
