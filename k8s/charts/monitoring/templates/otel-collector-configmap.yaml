{{- if .Values.configMaps.otelCollectorConfig.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.otelCollector.labels.app }}
  namespace: {{ .Values.global.namespace }}
data:
  otel-collector.yml: |
    receivers:
      filelog:
        include:
          - /var/log/pods/*/*/*.log
        start_at: beginning

      # OTLP 수신 (Spring Boot에서 push)
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

      # OTel 자체 메트릭
      prometheus:
        config:
          scrape_configs:
            - job_name: otel-collector-self
              static_configs:
                - targets: ["127.0.0.1:8888"]

    processors:
      k8sattributes:
        auth_type: serviceAccount
        extract:
          metadata:
            - k8s.namespace.name
            - k8s.pod.name
            - k8s.container.name
            - k8s.node.name

      attributes:
        actions:
          - key: env
            from_attribute: k8s.namespace.name
            action: upsert
<<<<<<< HEAD

      resource:
        attributes:
          - action: insert
            key: loki.resource.labels
            value: k8s_namespace_name,k8s_pod_name,k8s_container_name
=======
          - key: loki.attribute.labels
            action: insert
            value: k8s.namespace.name,k8s.pod.name,k8s.container.name
>>>>>>> da1cb30 (모니터링: OTel 로그 Loki 스트림 분산 라벨 수정)

      batch:
        timeout: 5s
        send_batch_size: 1024

    exporters:
      loki:
<<<<<<< HEAD
        endpoint: http://{{ .Values.global.namespace }}-loki:{{ .Values.loki.service.port }}/loki/api/v1/push
      prometheus:
        endpoint: "0.0.0.0:8889" # 🔥 Prometheus가 scrape할 유일한 엔드포인트
        
    service: 
      pipelines:
        logs:
          receivers: [filelog] # 파일에서 로그 수집
          processors: [k8sattributes, attributes, resource, batch]
          exporters: [loki] # Loki로 전송
        metrics:
          receivers: [otlp, prometheus] # OTLP + 자체 메트릭
          processors: [k8sattributes, batch]
          exporters: [prometheus] # Prometheus exporter로 노출
=======
        endpoint: http://{{ .Values.global.namespace }}-loki:3100/loki/api/v1/push

    service:
      pipelines:
        logs:
          receivers: [filelog]
          processors:
            - k8sattributes
            - attributes
            - batch
          exporters: [loki]
>>>>>>> da1cb30 (모니터링: OTel 로그 Loki 스트림 분산 라벨 수정)
{{- end }}
