global:
  namespace: release
  imagePullSecrets:
    - name: ghcr-secret

# =========================
# Server (Spring Boot)
# =========================
server:
  enabled: true
  replicas: 1

  image:
    repository: ghcr.io/sw-campus/sw-campus-server
    tag: "v1.0.7"
    pullPolicy: IfNotPresent

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 300m
      memory: 512Mi

  # Spring profile
  configMap:
    name: sw-campus-server-config-release

  # ✅ Redis Sidecar (Server 파드에 포함)
  redis:
    enabled: true
    image: redis:7-alpine

  # 공통 서버 시크릿 (JWT, MAIL 등)
  secret:
    enabled: true
    name: sw-campus-server-secret-release
  
  # DB / 외부 연동 Secret
  dbSecret:
   enabled: true
   name: sw-campus-db-secret-release

  # AWS
  awsSecret:
    enabled: true
    name: sw-campus-aws-secret-release

  # OAuth
  oauthSecret:
    enabled: true
    name: sw-campus-oauth-secret-release

  # Google Analytics
  gaSecret:
    enabled: true
    name: ga-credentials-release

  # SSH 터널링용 Secret
  infraSecret:
    enabled: false

  # 환경변수 설정
  env:
    - name: MANAGEMENT_METRICS_EXPORT_OTLP_ENABLED
      value: "false"  # ❌ OTLP 메트릭스 export 비활성화 (연결 실패 에러 방지)


# =========================
# Client (React)
# =========================
client:
  enabled: true
  replicas: 1

  image:
    repository: ghcr.io/sw-campus/sw-campus-client
    tag: "v1.0.13"
    pullPolicy: IfNotPresent

  service:
    type: ClusterIP
    port: 3000

  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

  configMap:
    name: sw-campus-client-config

  # Gemini API Key Secret
  secret:
    enabled: true
    name: sw-campus-client-secret-release


# =========================
# AI Service
# =========================
ai:
  enabled: true
  replicas: 1

  image:
    repository: ghcr.io/sw-campus/sw-campus-ai
    tag: "v1.0.7"
    pullPolicy: IfNotPresent

  service:
    type: ClusterIP
    port: 8000

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 300m
      memory: 512Mi


# =========================
# Database (External)
# =========================
database:
  enabled: false

# =========================
# Ingress settings (기본값 오버라이드)
# =========================
ingress:
  enabled: false
  className: alb

  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    # ⚠️ HTTPS(443)는 AWS 콘솔에서 수동 관리! 여기서는 HTTP만!
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
    # ssl-redirect는 AWS 콘솔에서 수동 설정 (여기서 설정하면 충돌 가능)
    # 헬스체크 설정
    alb.ingress.kubernetes.io/healthcheck-path: /
    alb.ingress.kubernetes.io/healthcheck-protocol: HTTP
    alb.ingress.kubernetes.io/healthcheck-port: traffic-port
    alb.ingress.kubernetes.io/success-codes: "200"

  # ⚠️ 인증서는 AWS 콘솔에서 443 리스너에 직접 연결!
  # 여기에 ARN 넣으면 퍼블릭 레포에 노출되므로 비워둠
  certificateArn: ""

  # ⚠️ 임시: 도메인 연동 전까지 모든 호스트 허용 (와일드카드)
  # 내일 도메인 연동 시 아래 주석을 해제하고 실제 도메인으로 변경
  hosts:
    # - host: softwarecampus.co.kr
    #   paths:
    #     - path: /
    #       pathType: Prefix
    #       service:
    #         name: sw-campus-client
    #         port: 3000
    #     - path: /api
    #       pathType: Prefix
    #       service:
    #         name: sw-campus-server
    #         port: 8080
    # - host: www.softwarecampus.co.kr
    #   paths:
    #     - path: /
    #       pathType: Prefix
    #       service:
    #         name: sw-campus-client
    #         port: 3000
    #     - path: /api
    #       pathType: Prefix
    #       service:
    #         name: sw-campus-server
    #         port: 8080
    # 임시: 모든 호스트 허용 (host 필드 없음)
    - host: ""
      paths:
        - path: /
          pathType: Prefix
          service:
            name: sw-campus-client
            port: 3000
        - path: /api
          pathType: Prefix
          service:
            name: sw-campus-server
            port: 8080
        - path: /ocr
          pathType: Prefix
          service:
            name: sw-campus-ai
            port: 8000
