# Release monitoring environment values
global:
  namespace: monitoring

monitoring:
  targetNamespace: release

grafana:
  replicas: 1

prometheus:
  replicas: 2

loki:
  replicas: 2

otelCollector:
  replicas: 2
  debug: false # 운영: 성능을 위해 logging exporter 비활성화
