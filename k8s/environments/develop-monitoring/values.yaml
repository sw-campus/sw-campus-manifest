# Develop monitoring environment values
global:
  namespace: monitoring

monitoring:
  targetNamespace: develop

prometheus:
  replicas: 2

loki:
  replicas: 2

otelCollector:
  debug: true # 개발: 디버깅을 위해 logging exporter 활성화
