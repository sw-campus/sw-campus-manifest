apiVersion: v1
kind: Service
metadata:
  name: sw-campus-server
  namespace: default
  labels:
    app: sw-campus-server
spec:
  selector:
    app: sw-campus-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP