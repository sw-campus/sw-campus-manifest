apiVersion: apps/v1
kind: Deployment
metadata:
  name: sw-campus-server
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: server
          image: ghcr.io/sw-campus/sw-campus-server:staging
