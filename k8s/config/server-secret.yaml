# ⚠️ 실제 Secret 파일은 /config/server-secret.yaml에 있습니다!
# 이 파일은 템플릿입니다. 실제 사용 시에는 config 디렉토리의 파일을 사용하세요.
#
# 사용 방법:
# kubectl apply -f ../../config/server-secret.yaml
#
# 또는 kubectl로 직접 생성:
# kubectl create secret generic sw-campus-server-secret \
#   --from-literal=SPRING_DATASOURCE_PASSWORD='your-actual-password' \
#   --from-literal=JWT_SECRET='your-actual-jwt-secret' \
#   -n default


