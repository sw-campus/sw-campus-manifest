apiVersion: apps/v1
kind: Deployment
metadata:
  name: sw-campus-ai
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sw-campus-ai
  template:
    metadata:
      labels:
        app: sw-campus-ai
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: ai
          image: ghcr.io/sw-campus/sw-campus-ai:115a2cf70aa63a4b4340478ce46215f1e88199ae
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
